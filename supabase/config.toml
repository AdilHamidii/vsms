project_id = "virtualsim"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 17

[db.pooler]
enabled = false

[realtime]
enabled = false

[studio]
enabled = true
port = 54323

[inbucket]
enabled = true
port = 54324

[storage]
enabled = false

[auth]
enabled = true
site_url = "relay://auth"
additional_redirect_urls = ["relay://auth"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = true
enable_anonymous_sign_ins = false

[auth.email]
enable_signup = false
double_confirm_changes = true
enable_confirmations = false

[auth.external.apple]
# For native iOS Sign in with Apple we exchange an Apple-signed ID token via
# /auth/v1/token?grant_type=id_token. Supabase verifies the token against
# Apple's public JWKS — no client secret needed.
# The `client_id` here is the iOS bundle ID and matches the `aud` claim
# Supabase expects on incoming tokens.
enabled = true
client_id = "com.anthersystems.VirtualSIM"
skip_nonce_check = false

[edge_runtime]
enabled = true
policy = "oneshot"
inspector_port = 8083

[analytics]
enabled = false
